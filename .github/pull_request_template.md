## 무엇을 왜

<!-- 한두 줄. 이슈가 있으면 #번호 -->

## 어떻게

<!-- 접근 방식, 대안이 있었다면 왜 이걸 골랐는지 -->

## 판정 영향

<!-- 데이터·판정 로직을 건드렸다면 필수. 안 건드렸으면 "없음" -->
- 정규 / 근사 / 일부부족 / 불가:  →
- 의도한 변화인 이유:

## 확인

- [ ] `npm test` 통과
- [ ] `npm run build` 통과
- [ ] 실제 화면에서 확인 (해당되면 스크린샷)
- [ ] `seed.ts` 를 손으로 고치지 않았다 (변환은 `scripts/` 로)
